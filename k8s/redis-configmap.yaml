apiVersion: v1
kind: ConfigMap
metadata:
  name: app-redis-config
data:
  # key   is file name
  # value is file content
  redis.conf: |
    bind 0.0.0.0
    port 6379
    protected-mode no
    appendonly yes
    dir /redis/data